<h1>Listing photos</h1>

<table>
  <tr>
  <!--  <th>Name</th>
    <th>Description</th> -->
    <th>Date</th>
    <th></th>
   
  </tr>

<% @photos.each do |photo| %>
  <tr>
   <!-- <td><%= photo.name %></td>
    <td><%= photo.description %></td>-->
    <td><%= photo.dt %></td>
    <td><%= link_to 'View', :controller => 'photos', :action => 'public_user_show', :username => params[:username], :id => photo.id %></td>
   
  </tr>
<% end %>
</table>

<br />

